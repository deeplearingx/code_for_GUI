# 算法设计说明文档

## 1. 方案概述

本参赛方案面向安卓 GUI 自动操作任务，构建了一个基于视觉大模型的规则增强型 Agent。整体设计遵循“任务解析—动作生成—动作校验—策略修正—完成判定”的处理链路，在尽量保持系统简洁的前提下，通过少量高收益规则提升任务执行稳定性与泛化能力。

与直接依赖模型端到端生成整条操作轨迹不同，本方案将重点放在“生成当前最合理的一步动作”上。Agent 每轮结合当前截图、用户指令和有限历史动作，输出严格符合评测要求的标准动作：`CLICK`、`TYPE`、`SCROLL`、`OPEN`、`COMPLETE`。

## 2. 总体架构

提交代码由 `agent.py` 与 `utils/` 中的辅助模块共同组成，职责划分如下：

### 2.1 `agent.py`

`agent.py` 是主控制模块，负责串联完整决策流程，包括：

- 初始化任务状态与动作历史
- 在首步直接输出 `OPEN`
- 组织提示词与截图输入，调用视觉模型
- 解析模型输出为标准动作
- 执行动作校验与策略修正
- 在必要时进行重试或回退
- 更新历史状态并返回最终动作

### 2.2 `utils/task_parser.py`

负责从用户自然语言指令中抽取结构化任务信息，包括：

- 目标 App 名称
- 任务类型
- 搜索关键词
- 评论内容
- 出发地 / 目的地
- 店铺名 / 商品名
- 待输入文本队列

当前支持的视频搜索、评论、地图、外卖、旅行等任务，都统一映射到同一个 `TaskInfo` 结构中进行管理。

### 2.3 `utils/prompt_builder.py`

负责构造发给视觉模型的提示词与消息结构，将以下信息组织进模型输入：

- 用户原始任务
- 当前待输入内容
- 已执行历史动作摘要
- 当前步数
- 当前截图

同时，该模块明确限定输出格式必须为标准 JSON，以减少非结构化回答带来的解析失败。

### 2.4 动作处理相关模块

- `utils/action_parser.py`：将模型原始输出解析为标准动作对象
- `utils/action_validator.py`：执行动作标准化与合法性校验
- `utils/action_policy.py`：对“格式合法但高概率错误”的动作进行后置修正
- `utils/completion_policy.py`：控制 `COMPLETE` 的保守触发条件

### 2.5 辅助决策模块

- `utils/early_screen_policy.py`：处理开屏后早期稳定页面的确定性点击
- `utils/control_grounding.py`：维护不同 App 的语义坐标锚点
- `utils/history_manager.py`：维护历史动作摘要、重复点击检测等状态信息

## 3. 核心算法设计

### 3.1 任务解析与输入队列约束

GUI 自动操作任务中，一个常见问题是模型容易自由生成错误输入文本。为降低该类错误，本方案将用户任务先解析为结构化的待输入队列 `type_queue`，再由 Agent 在运行时逐项消费。

例如：

- 视频搜索任务：仅保留搜索关键词
- 评论任务：依次保留“搜索词 → 评论内容”
- 地图任务：依次保留“起点 → 终点”
- 外卖任务：依次保留“店铺名 → 商品名”
- 旅行任务：依次保留“出发地 → 目的地”

这种设计的优点是：

1. 输入内容来源于原始任务解析，而非模型自由编造
2. 多轮输入任务可以显式跟踪当前输入进度
3. 可以与后续动作校验和策略修正形成联动

因此，模型在执行 `TYPE` 时只能输入当前待输入文本，从而有效降低文本漂移与错输问题。

### 3.2 Prompt 设计

为了让视觉模型输出更加稳定，本方案采用了强约束 Prompt 设计。系统提示词中明确规定：

- 只能输出一个 JSON 对象
- 动作空间仅限标准五类动作
- 坐标采用 0~1000 的归一化表示
- 不允许输出 Markdown、解释性文字或非标准调用格式

此外，Prompt 中还注入任务目标、待输入进度、历史动作和当前步数，以帮助模型在局部决策时具备足够上下文。

该设计兼顾了模型的视觉理解能力与结构化输出要求，是整个系统稳定运行的基础。

### 3.3 动作校验机制

模型生成的候选动作不会被直接执行，而是必须经过动作校验器。校验器承担以下职责：

- 将动作名统一标准化，例如 `click` 统一为 `CLICK`
- 将参数名统一映射为标准字段，例如 `coord` 映射为 `point`
- 将坐标规范化到评测要求的 0~1000 区间
- 自动对 `TYPE`、`OPEN` 等动作补齐可推断参数
- 拒绝明显不合理的动作时机，例如：
  - `OPEN` 后直接 `TYPE`
  - 连续两次 `TYPE`
  - 在任务尚未完成时提前 `COMPLETE`

若动作不通过校验，则系统会给模型一次明确的重试机会；若重试后仍失败，则转入回退逻辑。通过这一设计，可显著减少因格式问题或基础逻辑错误导致的无效步骤。

### 3.4 后置策略修正机制

在实际 GUI 任务中，模型经常会产生“形式合法但意图错误”的动作，例如点击了错误的搜索区域，或在输入框未激活时提前执行 `TYPE`。对此，本方案引入了后置动作策略修正层。

该模块仅在高置信、低风险场景下进行保守修正，典型情况包括：

- 搜索任务仍有待输入文本，但模型点击了明显偏离搜索框的区域
- 视频类任务在 `OPEN` 后应先点击搜索入口，而不是直接点搜索输入框或内容区
- 去哪儿旅行任务中，若顶部搜索栏尚未激活，则优先引导到搜索栏
- 百度地图、美团、旅行等流程型任务在 `OPEN` 后优先进入流程入口

修正后的动作会再次经过校验器验证；如果修正结果未通过校验，则系统回退到模型原始合法动作，而不是强制执行修正动作。这样既保证了策略修正的收益，又避免其引入新的风险。

### 3.5 早期界面确定性策略

部分 App 在打开后的前几步交互非常稳定，例如：

- 视频应用通常先点搜索入口
- 去哪儿旅行通常先点出发地输入框
- 百度地图、美团通常先进入流程入口
- 若出现高置信弹窗，则优先关闭弹窗

因此，本方案在 `OPEN` 后的早期步骤中引入了轻量确定性策略，以减少模型在固定场景中的试探成本，并避免无效点击。

### 3.6 历史状态管理与卡住检测

为了在多轮交互中保持上下文一致性，系统维护了一个轻量级历史管理器，其主要功能包括：

- 生成紧凑的历史动作摘要，用于后续提示模型
- 记录最近一次动作与点击坐标
- 检测是否在多个连续步骤中重复点击近似位置

当系统判断 Agent 可能陷入重复点击时，会触发重试提示，要求模型重新观察截图并改变策略。该机制能够有效降低局部循环导致的失败率。

### 3.7 保守完成判定

`COMPLETE` 是 GUI 任务中代价较高的动作，一旦触发过早，整个任务将直接失败。因此，本方案对完成判定进行了严格约束：

- 步数过早时不允许 `COMPLETE`
- 仍有待输入文本时不允许 `COMPLETE`
- `OPEN` 后不允许立即 `COMPLETE`
- 当前动作与最近动作序列需要满足可信的完成模式

通过这种保守判定机制，可以有效降低“任务尚未完成却提前结束”的问题。

## 4. 回退机制

在以下场景中，系统会启用回退机制：

- 模型调用失败
- 模型输出无法解析
- 校验失败且重试后仍未得到合法动作

回退机制并不试图覆盖所有任务逻辑，而是提供简单、低风险、可继续推进任务的默认动作。例如重新点击搜索入口、流程入口或搜索框。该机制的主要目标是避免系统在异常情况下直接停滞。

## 5. 方案特点与优势

本参赛方案具有以下特点：

1. **结构清晰**：模型推理、任务解析、动作校验和策略修正之间职责分明
2. **规则适度**：不对单个样例硬编码完整轨迹，而是抽象为任务类型、输入队列和语义锚点
3. **输出稳定**：通过标准化解析、动作校验与重试机制降低格式错误率
4. **执行保守**：通过后置修正与完成判定机制降低高风险错误
5. **依赖轻量**：仅依赖 `openai` 与 `pillow` 两个第三方库，便于部署与提交

## 6. 运行环境说明

本方案依赖的第三方 Python 库较少，仅包括：

- `openai`
- `pillow`

其中：

- `openai` 用于调用主办方兼容的模型接口
- `pillow` 用于处理截图图像对象

因此，提交包整体体积较小，便于满足比赛提交限制。

## 7. 总结

综上，本方案采用了“视觉模型 + 规则增强”的混合式设计路线。系统一方面保留视觉模型对复杂界面的感知与理解能力，另一方面通过任务解析、动作校验、后置修正、历史管理和保守完成判定等机制，弥补纯模型驱动在 GUI 自动操作任务中的常见缺陷。

该方案具有结构清晰、依赖轻量、实现稳定、易于提交的特点，能够较好适配主办方给定的评测接口与运行环境。
